{
  "name": "nov-hub",
  "description": "Plugin dependency resolver — detects environment, resolves capabilities, auto-selects providers, generates install plans",
  "version": "1.0.0",
  "author": {
    "name": "Thatcher",
    "url": "https://github.com/ThatcherT"
  },
  "repository": "https://github.com/ThatcherT/nov-hub",
  "license": "MIT",
  "keywords": ["dependencies", "resolver", "capabilities", "framework", "environment"],
  "mcpServers": {
    "nov-hub": {
      "command": "python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.py"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
